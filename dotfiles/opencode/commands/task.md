---
description: Create a new task on the Product backlog
---

Load the github-backlog skill first.

Create a new issue on the ExopenGitHub Product backlog (project #6).

Follow the skill's hard rules and workflow steps exactly:
1. Determine the correct repository from the task description
2. Create a real issue with `gh issue create` (never a draft)
3. Add it to the project board
4. Set Status to "Ready for development" and set the appropriate Initiative
5. Assign to androidand unless told otherwise

Use the issue body template from the skill for the description.

Task: $ARGUMENTS
